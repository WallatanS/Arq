import 'package:flutter/material.dart';

class especifica extends StatelessWidget {
  const especifica({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
